// South African Quiz Database - V2
const saQuizDatabase = {
    culture: [
        {
            question: "What is the traditional South African greeting that means 'I see you'?",
            type: "text",
            answer: "Sawubona"
        },
        {
            question: "Which of these is a traditional South African music genre?",
            type: "multiple",
            options: ["Amapiano", "K-pop", "Reggaeton", "Samba"],
            correct: 0
        },
        {
            question: "What does the Zulu word 'Ubuntu' mean?",
            type: "multiple",
            options: ["A computer", "I am because we are", "South Africa", "A dance"],
            correct: 1
        },
        {
            question: "The xhosa language is known for its unique click consonants.",
            type: "truefalse",
            correct: true
        },
        {
            question: "Which South African ethnic group is known for their colorful beadwork?",
            type: "multiple",
            options: ["Sotho", "Ndebele", "Venda", "Tsonga"],
            correct: 1
        },
        {
            question: "What is the name of the traditional South African round house?",
            type: "text",
            answer: "Rondavel"
        },
        {
            question: "Gumboot dancing originated in South African mines.",
            type: "truefalse",
            correct: true
        },
        {
            question: "Which instrument is central to traditional Zulu music?",
            type: "multiple",
            options: ["Didgeridoo", "Uhadi", "Sitar", "Banjo"],
            correct: 1
        },
        {
            question: "What is Shebeen culture in South Africa?",
            type: "multiple",
            options: ["Government buildings", "Informal taverns or bars", "Shopping centers", "Sports clubs"],
            correct: 1
        },
        {
            question: "The Afrikaans language has its roots in Dutch and other languages.",
            type: "truefalse",
            correct: true
        }
    ],
    history: [
        {
            question: "In what year did South Africa transition to democracy?",
            type: "multiple",
            options: ["1991", "1993", "1994", "1996"],
            correct: 2
        },
        {
            question: "Who was the first president of democratic South Africa?",
            type: "text",
            answer: "Nelson Mandela"
        },
        {
            question: "The Apartheid system officially ended in 1991.",
            type: "truefalse",
            correct: true
        },
        {
            question: "What is Robben Island famous for in South African history?",
            type: "multiple",
            options: ["A beach resort", "A political prison", "A nature reserve", "A university"],
            correct: 1
        },
        {
            question: "Which leader signed the end of Apartheid?",
            type: "text",
            answer: "F.W. de Klerk"
        },
        {
            question: "The Sharpeville Massacre occurred in 1960 when police shot protesters.",
            type: "truefalse",
            correct: true
        },
        {
            question: "What was the primary purpose of the Truth and Reconciliation Commission?",
            type: "multiple",
            options: ["Economic development", "To document and address apartheid crimes", "Tourism promotion", "Education reform"],
            correct: 1
        },
        {
            question: "In what year was the South African Constitution adopted?",
            type: "multiple",
            options: ["1993", "1994", "1996", "1998"],
            correct: 2
        },
        {
            question: "Soweto stands for South Western Townships.",
            type: "truefalse",
            correct: true
        },
        {
            question: "The Soweto Uprising took place in which year?",
            type: "multiple",
            options: ["1960", "1976", "1985", "1990"],
            correct: 1
        }
    ],
    food: [
        {
            question: "What is Bunny Chow, a popular South African street food?",
            type: "multiple",
            options: ["A meat pie", "Curry served in bread", "A vegetable stew", "A fish dish"],
            correct: 1
        },
        {
            question: "What is Biltong?",
            type: "text",
            answer: "Dried cured meat"
        },
        {
            question: "Pap is made from maize and is a staple food in South Africa.",
            type: "truefalse",
            correct: true
        },
        {
            question: "What traditional South African dish consists of meat and offal?",
            type: "multiple",
            options: ["Bunny Chow", "Koeksister", "Boerewors", "Sago pudding"],
            correct: 2
        },
        {
            question: "What is a Koeksister?",
            type: "multiple",
            options: ["A type of cheese", "A sweet fried pastry", "A fermented beverage", "A vegetable"],
            correct: 1
        },
        {
            question: "Braai is the South African tradition of barbecuing meat.",
            type: "truefalse",
            correct: true
        },
        {
            question: "What is Bobotie?",
            type: "multiple",
            options: ["A drink", "A spiced mince dish with egg topping", "A type of bread", "A dessert"],
            correct: 1
        },
        {
            question: "What is Sosaties?",
            type: "text",
            answer: "Grilled meat skewers"
        },
        {
            question: "Mealie pap is made from which grain?",
            type: "multiple",
            options: ["Wheat", "Rice", "Maize", "Barley"],
            correct: 2
        },
        {
            question: "Gatsby is a popular sandwich from Cape Town.",
            type: "truefalse",
            correct: true
        }
    ],
    cars: [
        {
            question: "Which car manufacturer has a major production facility in South Africa?",
            type: "multiple",
            options: ["Tesla", "Toyota", "Rolls-Royce", "Ferrari"],
            correct: 1
        },
        {
            question: "What is the name of the South African sports car brand Optimal Energy was developing?",
            type: "text",
            answer: "Optimal Energy"
        },
        {
            question: "Optimal Energy was South Africa's first electric sports car project.",
            type: "truefalse",
            correct: true
        },
        {
            question: "Which international car brand has assembly plants in South Africa?",
            type: "multiple",
            options: ["Volkswagen", "BMW", "Mercedes-Benz", "All of the above"],
            correct: 3
        },
        {
            question: "What is the major car manufacturing hub in South Africa?",
            type: "text",
            answer: "Durban"
        },
        {
            question: "South Africa is one of Africa's largest car manufacturers.",
            type: "truefalse",
            correct: true
        },
        {
            question: "Which Japanese car brand has significant manufacturing in South Africa?",
            type: "multiple",
            options: ["Honda", "Nissan", "Hyundai", "Subaru"],
            correct: 1
        },
        {
            question: "The Nissan Datsun brand was revived and manufactured in South Africa.",
            type: "truefalse",
            correct: true
        },
        {
            question: "What percentage of South Africa's manufacturing exports are from the automotive sector?",
            type: "multiple",
            options: ["Around 15%", "Around 35%", "Around 55%", "Around 75%"],
            correct: 2
        },
        {
            question: "Which luxury car manufacturer has a production plant in South Africa?",
            type: "multiple",
            options: ["Lamborghini", "Porsche", "BMW", "Bugatti"],
            correct: 2
        }
    ]
};

// Game State
let gameState = {
    currentQuestionIndex: 0,
    score: 0,
    difficulty: 'easy',
    category: 'culture',
    answers: [],
    timePerQuestion: 10,
    timerInterval: null,
    timeRemaining: 10,
    answered: false,
    currentQuestions: []
};

// Difficulty Settings
const difficultySettings = {
    easy: { timePerQuestion: 10, multiplier: 1 },
    medium: { timePerQuestion: 7, multiplier: 1.5 },
    hard: { timePerQuestion: 5, multiplier: 2 }
};

// Leaderboard
let leaderboard = JSON.parse(localStorage.getItem('saQuizLeaderboard')) || [];

// Screen Management
function switchScreen(screenName) {
    document.querySelectorAll('.screen').forEach(screen => {
        screen.classList.remove('active');
    });
    document.getElementById(screenName).classList.add('active');
}

// Select Category
function selectCategory(category) {
    gameState.category = category;
    const categoryNames = {
        culture: '🎭 South African Culture',
        history: '📚 SA History',
        food: '🍲 Traditional Food',
        cars: '🚗 SA Car Brands'
    };
    alert(`Category selected: ${categoryNames[category]}\nNow choose your difficulty level!`);
}

// Start Quiz
function startQuiz(difficulty) {
    if (!gameState.category) {
        alert('Please select a category first!');
        return;
    }

    gameState.difficulty = difficulty;
    gameState.currentQuestionIndex = 0;
    gameState.score = 0;
    gameState.answers = [];
    gameState.timePerQuestion = difficultySettings[difficulty].timePerQuestion;
    gameState.answered = false;
    gameState.currentQuestions = [...saQuizDatabase[gameState.category]].sort(() => Math.random() - 0.5);

    document.getElementById('totalQuestions').textContent = gameState.currentQuestions.length;
    document.getElementById('difficultyIndicator').textContent = 
        difficulty.charAt(0).toUpperCase() + difficulty.slice(1);
    
    const categoryNames = {
        culture: 'Culture',
        history: 'History',
        food: 'Food',
        cars: 'Car Brands'
    };
    document.getElementById('categoryIndicator').textContent = categoryNames[gameState.category];
    document.getElementById('finalDifficulty').textContent = 
        difficulty.charAt(0).toUpperCase() + difficulty.slice(1);

    switchScreen('quizScreen');
    loadQuestion();
}

// Load Question
function loadQuestion() {
    const question = gameState.currentQuestions[gameState.currentQuestionIndex];
    const questionNumber = gameState.currentQuestionIndex + 1;

    document.getElementById('questionNumber').textContent = questionNumber;
    document.getElementById('questionText').textContent = question.question;
    document.getElementById('currentScore').textContent = gameState.score;

    // Category tag
    const categoryEmojis = {
        culture: '🎭',
        history: '📚',
        food: '🍲',
        cars: '🚗'
    };
    document.getElementById('categoryTag').textContent = categoryEmojis[gameState.category] + ' ' + gameState.category.toUpperCase();

    // Reset UI
    document.getElementById('multipleChoice').innerHTML = '';
    document.getElementById('multipleChoice').style.display = 'none';
    document.getElementById('trueFalse').style.display = 'none';
    document.getElementById('textInput').style.display = 'none';
    document.getElementById('textAnswer').value = '';

    gameState.answered = false;

    // Load question based on type
    if (question.type === 'multiple') {
        loadMultipleChoice(question);
    } else if (question.type === 'truefalse') {
        loadTrueFalse();
    } else if (question.type === 'text') {
        loadTextInput();
    }

    // Start timer
    startTimer();
}

// Load Multiple Choice
function loadMultipleChoice(question) {
    const container = document.getElementById('multipleChoice');
    container.style.display = 'block';

    question.options.forEach((option, index) => {
        const button = document.createElement('button');
        button.className = 'btn btn-answer';
        button.textContent = option;
        button.onclick = () => answerQuestion(index);
        container.appendChild(button);
    });
}

// Load True/False
function loadTrueFalse() {
    document.getElementById('trueFalse').style.display = 'flex';
}

// Load Text Input
function loadTextInput() {
    document.getElementById('textInput').style.display = 'block';
    document.getElementById('textAnswer').focus();
}

// Answer Question
function answerQuestion(selectedAnswer) {
    if (gameState.answered) return;

    gameState.answered = true;
    const question = gameState.currentQuestions[gameState.currentQuestionIndex];
    let isCorrect = false;

    if (question.type === 'multiple' || question.type === 'truefalse') {
        isCorrect = selectedAnswer === question.correct;
    }

    recordAnswer(isCorrect);
    showFeedback(isCorrect);
}

// Submit Text Answer
function submitTextAnswer() {
    if (gameState.answered) return;

    gameState.answered = true;
    const question = gameState.currentQuestions[gameState.currentQuestionIndex];
    const userAnswer = document.getElementById('textAnswer').value.trim().toLowerCase();
    const correctAnswer = question.answer.toLowerCase();
    const isCorrect = userAnswer === correctAnswer;

    recordAnswer(isCorrect);
    showTextFeedback(isCorrect, correctAnswer);
}

// Handle Text Input Keypress
function handleTextInputKeypress(event) {
    if (event.key === 'Enter') {
        submitTextAnswer();
    }
}

// Record Answer
function recordAnswer(isCorrect) {
    clearInterval(gameState.timerInterval);

    if (isCorrect) {
        const multiplier = difficultySettings[gameState.difficulty].multiplier;
        const points = Math.ceil(10 * multiplier);
        gameState.score += points;
    }

    gameState.answers.push({
        question: gameState.currentQuestions[gameState.currentQuestionIndex].question,
        isCorrect: isCorrect
    });
}

// Show Feedback
function showFeedback(isCorrect) {
    const buttons = document.querySelectorAll('.btn-answer');
    buttons.forEach((btn, index) => {
        if (index === gameState.currentQuestions[gameState.currentQuestionIndex].correct) {
            btn.classList.add('correct');
        }
        btn.disabled = true;
        btn.style.pointerEvents = 'none';
    });

    setTimeout(() => {
        if (gameState.currentQuestionIndex < gameState.currentQuestions.length - 1) {
            gameState.currentQuestionIndex++;
            loadQuestion();
        } else {
            endQuiz();
        }
    }, 1500);
}

// Show Text Feedback
function showTextFeedback(isCorrect, correctAnswer) {
    const container = document.getElementById('textInput');
    const feedback = document.createElement('div');
    feedback.style.marginTop = '10px';
    feedback.style.fontSize = '1.1em';
    feedback.style.fontWeight = 'bold';

    if (isCorrect) {
        feedback.textContent = '✓ Correct!';
        feedback.style.color = '#27ae60';
    } else {
        feedback.textContent = `✗ Incorrect! Answer: ${correctAnswer}`;
        feedback.style.color = '#e74c3c';
    }

    container.appendChild(feedback);
    document.getElementById('textAnswer').disabled = true;
    document.querySelectorAll('#textInput .btn').forEach(btn => btn.disabled = true);

    setTimeout(() => {
        if (gameState.currentQuestionIndex < gameState.currentQuestions.length - 1) {
            gameState.currentQuestionIndex++;
            loadQuestion();
        } else {
            endQuiz();
        }
    }, 1500);
}

// Timer
function startTimer() {
    gameState.timeRemaining = gameState.timePerQuestion;
    updateTimerDisplay();

    gameState.timerInterval = setInterval(() => {
        gameState.timeRemaining--;
        updateTimerDisplay();

        if (gameState.timeRemaining <= 3) {
            document.getElementById('timer').classList.add('warning');
        }

        if (gameState.timeRemaining <= 0) {
            clearInterval(gameState.timerInterval);
            if (!gameState.answered) {
                gameState.answered = true;
                recordAnswer(false);
                showTimeoutFeedback();
            }
        }
    }, 1000);
}

function updateTimerDisplay() {
    document.getElementById('timer').textContent = gameState.timeRemaining + 's';
}

function showTimeoutFeedback() {
    document.getElementById('timer').classList.remove('warning');
    const buttons = document.querySelectorAll('.btn-answer');
    buttons.forEach(btn => {
        btn.disabled = true;
        btn.style.pointerEvents = 'none';
    });

    // Show correct answer for multiple choice
    const question = gameState.currentQuestions[gameState.currentQuestionIndex];
    if (question.type === 'multiple' && buttons.length > 0) {
        buttons[question.correct].classList.add('correct');
    }

    setTimeout(() => {
        if (gameState.currentQuestionIndex < gameState.currentQuestions.length - 1) {
            gameState.currentQuestionIndex++;
            loadQuestion();
        } else {
            endQuiz();
        }
    }, 1500);
}

// End Quiz
function endQuiz() {
    clearInterval(gameState.timerInterval);
    const correctCount = gameState.answers.filter(a => a.isCorrect).length;

    document.getElementById('finalScore').textContent = gameState.score;
    document.getElementById('correctCount').textContent = correctCount;

    switchScreen('resultsScreen');
}

// Save Score
function saveScore() {
    const playerName = document.getElementById('playerName').value.trim() || 'Anonymous';
    
    const score = {
        name: playerName,
        score: gameState.score,
        difficulty: gameState.difficulty,
        category: gameState.category,
        correctAnswers: gameState.answers.filter(a => a.isCorrect).length,
        totalQuestions: gameState.currentQuestions.length,
        date: new Date().toLocaleDateString()
    };

    leaderboard.push(score);
    localStorage.setItem('saQuizLeaderboard', JSON.stringify(leaderboard));

    showLeaderboard();
}

// Show Leaderboard
function showLeaderboard() {
    switchScreen('leaderboardScreen');
    switchLeaderboardTab('all');
}

// Switch Leaderboard Tab
function switchLeaderboardTab(tab) {
    document.querySelectorAll('.tab-btn').forEach(btn => btn.classList.remove('active'));
    event.target.classList.add('active');

    let filteredScores = leaderboard;

    if (tab !== 'all') {
        filteredScores = leaderboard.filter(score => score.category === tab);
    }

    // Sort by score (descending)
    filteredScores.sort((a, b) => b.score - a.score);

    // Display leaderboard
    const leaderboardList = document.getElementById('leaderboardList');
    leaderboardList.innerHTML = '';

    if (filteredScores.length === 0) {
        leaderboardList.innerHTML = '<div class="empty-message">No scores yet. Play a quiz!</div>';
        return;
    }

    filteredScores.forEach((score, index) => {
        const rank = index + 1;
        const item = document.createElement('div');
        item.className = `leaderboard-item rank-${rank <= 3 ? rank : 'default'}`;

        const medal = rank === 1 ? '🥇' : rank === 2 ? '🥈' : rank === 3 ? '🥉' : '';
        
        const categoryEmojis = {
            culture: '🎭',
            history: '📚',
            food: '🍲',
            cars: '🚗'
        };

        item.innerHTML = `
            <div class="rank-badge">${medal || rank}</div>
            <div class="player-info">
                <div class="player-name">${score.name}</div>
                <div class="player-meta">
                    ${categoryEmojis[score.category]} ${score.category} | ${score.difficulty.toUpperCase()} | ${score.correctAnswers}/${score.totalQuestions} correct | ${score.date}
                </div>
            </div>
            <div class="player-score">${score.score} pts</div>
        `;

        leaderboardList.appendChild(item);
    });
}

// Go Home
function goHome() {
    gameState.category = null;
    switchScreen('homeScreen');
}

// Initialize
document.addEventListener('DOMContentLoaded', () => {
    switchScreen('homeScreen');
});